import React from 'react';

import Logo from '../Logo';
import Mark from '../Mark';

const Header = () => {
  return (
    <div>
      <Logo />
      <Mark />
    </div>
  );
};

export default Header;
